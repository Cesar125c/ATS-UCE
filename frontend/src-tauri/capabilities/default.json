{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the ATS-UCE desktop and mobile shell.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
